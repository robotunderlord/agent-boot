# agent-boot — the can's internals.
#
# WHAT THIS DEMONSTRATES, AND WHAT IT DELIBERATELY DOES NOT
# ---------------------------------------------------------
# agent-boot itself has NO runtime dependencies — pure standard library. So this compose is not
# here to install it; it is here to show the one structural idea that makes a containerised agent
# work at all:
#
#     THE IMAGE IS THE VESSEL. THE MOUNTS ARE THE BEING.
#
# The image carries code and doctrine templates. It carries NO secrets, NO memory and NO local
# rules. Everything that makes a particular agent *that* agent is mounted in at runtime, which is
# what lets one image serve every seat and lets a seat survive being rebuilt.
#
# Three mounts, three jobs:
#
#   ~/.claude      settings.json (the three hook layers) + memory namespace   rw — it writes here
#   ~/.agentboot   nag.md · nag-terse.md · tattoo.md — the operator's own      rw — the tattoo is live state
#   tools.d/       JSON manifests declaring what THIS environment offers      ro — discovery input
#
# The third is the interesting one. The image cannot know that this particular seat has a vector
# store, or a credential CLI, or a browser grid. Hardcoding those makes the image unportable, and
# omitting them makes the agent blind to its own environment. So the environment DECLARES them in
# tools.d and the boot DISCOVERS them — and then probes every one before letting the agent believe
# it has them (see agentboot/tools.py).
#
# There are no sidecars here on purpose. A compose full of services that this repo does not need
# would be aspirational, and an aspirational compose is a lie that takes an afternoon to discover.
# Add your own; the discovery contract is the JSON manifest, not a service name.

services:
  agent:
    build: .
    image: agent-boot:0.2.0
    container_name: agent-boot
    init: true
    restart: unless-stopped

    # The agent sits on BOTH planes - the only thing that does. Like a routing engine bridging the
    # backplane and the front panel, it is the single point where internal state and the outside
    # world meet, which is exactly why the door on it is TLS'd and credentialed.
    networks:
      backplane:
        # INTERNAL DNS, the lightest possible kind: docker's embedded resolver (127.0.0.11)
        # already answers for names on a user-defined bridge. An alias costs nothing and buys a
        # stable internal name.
        #
        # `.local` ON PURPOSE, and NOT the estate's internal zone. A `.int` name would imply the
        # estate's DNS servers - which this plane cannot reach at all (no route out), and whose
        # namespace these names would collide with. `.local` says what is true: link-local, answered
        # only by the local resolver, meaningless one hop away.
        aliases:
          - agent.local
      frontpanel:
        aliases:
          - agent.local

    # Least privilege for the plane that faces outward.
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

    environment:
      # os.pathsep-separated manifest paths, read at boot by ToolRegistry.discover().
      AGENTBOOT_TOOLS: /run/tools.d/environment.json
      CLAUDE_DIR: /home/agent/.claude
      AGENTBOOT_DIR: /home/agent/.agentboot
      # ONE session, and it IS the main loop. A second is a second AGENT writing the same running
      # thread - a split brain with no merge. Bounded side-work goes to a minion instead.
      AGENT_SESSION: main
      # What the layout actually runs inside that session.
      AGENT_MAIN_LOOP: bash
      ZELLIJ_CONFIG_DIR: /home/agent/.agentboot/zellij
      # The KEYED tier: ~1ms find-by-key, no embedder, no model involved.
      # Addressed by its link-local alias, resolved only by the embedded resolver on the backplane.
      AGENT_MONGO_URL: mongodb://memory.local:27017
      AGENT_MONGO_DB: agent
      # THE ROBOT'S OWN CA - internal to him, borrowed from nothing.
      #
      # Generated once at first boot into his state volume and reused forever after. Not the
      # estate's CA, not a public one: his. That matters for the same reason the rest of the can
      # does - he must work identically on a laptop, a burst GPU, or a machine that has never heard
      # of this lab.
      #
      # It buys the thing self-signed cannot: import his root ONCE into a browser, and every cert he
      # ever issues for himself is trusted after that. Self-signed encrypts but authenticates
      # nothing - a client cannot tell him from an impostor on the same address.
      #
      # The private key lives in the state volume at 0600 and never in the image. An image that
      # baked a CA key would make every copy of it able to impersonate every instance.
      AGENT_CA_DIR: /home/agent/.agentboot/ca
      # The SEMANTIC tier: nearest-meaning over the vault. Embeddings are computed LOCALLY - the
      # store refuses a remote embedder outright, because embedding a private corpus against a
      # hosted endpoint exports the whole corpus, once, silently, and it does not look like an
      # exfiltration in any log. Leave AGENT_EMBED_ENDPOINT unset, or point it at localhost.
      AGENT_CORPUS: /vault
      AGENT_RAG_STORE: /home/agent/.agentboot/rag
      AGENT_RAG_COLLECTION: vault
      # The embedding model reads from the PLANE, not from the image layer it was baked into.
      # Both are local, so both are "pre-connected" - but the plane version is shared between
      # instances, survives a rebuild, and does not ride along in every pull of the image.
      # The image still carries a copy as a FALLBACK, so an instance with no plane still works.
      XDG_CACHE_HOME: /home/agent/.agentboot/cache
      # TIER 1 - the resident brain. Small, CPU-only, on NO account.
      # Measured on this hardware: ~580ms for a short reply, ~25 tok/s. That is conversational for
      # acknowledgements and short turns, and deliberately NOT for essays - a long answer here takes
      # seconds, which is the signal to escalate rather than a defect.
      AGENT_LOCAL_LLM: http://brain.local:8080/v1
      AGENT_LOCAL_MODEL: qwen2.5-0.5b-instruct
      # TIER 2 - the pathway to the recruited regions. The spine carries signals; the regions think.
      AGENT_SPINE_URL: ${AGENT_SPINE_URL:-}
      # The browser terminal starts ONLY if a credential is supplied. Set it in an env file, never
      # here - a password in a committed compose is a password in the repo's history forever.
      # AGENT_WEB_CREDENTIAL: user:password    <- via `docker compose --env-file`
      AGENT_WEB_WRITABLE: "false"

    depends_on:
      mongo:
        condition: service_healthy
      # NOT service_healthy, deliberately. The agent must come up whether or not a brain is
      # attached: with none, the dispatcher degrades to recall-only and says so. An agent that
      # refuses to start for want of a model is worse than one that can still expand a crumb.
      brain:
        condition: service_started

    volumes:
      # --- the being: mounted, never baked ---
      # From the STORAGE PLANE, by subpath - one host device, many consumers.
      - type: volume
        source: agent_storage
        target: /home/agent/.claude
        volume: { subpath: claude }
      - type: volume
        source: agent_storage
        target: /home/agent/.agentboot
        volume: { subpath: agentboot }
      # Models live on the plane too, NOT in the image. A model is bytes; bytes go on the plane.
      - type: volume
        source: agent_storage
        target: /models
        volume: { subpath: models }
      # --- what this environment offers (read-only: the agent discovers, it does not edit) ---
      - ./tools.d:/run/tools.d:ro
      # --- the corpus: the knowledge base he indexes, mounted READ-ONLY ---
      # A Quartz/Obsidian-style markdown vault. Read-only on purpose: the agent INDEXES the vault,
      # it does not edit it. That keeps the knowledge base a source rather than a scratchpad, and it
      # means a bad index run can never damage the thing being indexed - which is what makes
      # re-indexing a habit rather than an event.
      - ${AGENT_VAULT:-./vault}:/vault:ro

    ports:
      - "${AGENT_WEB_PORT:-7681}:7681"

  # ── TIER 1: the resident brain ────────────────────────────────────────────────────────────────
  # Small, CPU-only, and on NO ACCOUNT. This is the autonomic layer - it answers acknowledgements
  # and short conversational turns so the main loop is instant, and escalates everything else.
  #
  # WHY THIS IS NOT A COMPROMISE (research/EXP-006): a SMALL model with the right context matched
  # or beat a LARGE model without it, at under half the tokens - while the large UNPRIMED one
  # cowboyed the errand and had to be stopped. Capability was never the binding constraint;
  # orientation was. The recall behind this model is what makes it adequate.
  #
  # MEASURED on an ordinary CPU: ~580ms for a short reply, ~25 tok/s. Conversational for what it is
  # for. A long answer takes seconds here, and that is the SIGNAL TO ESCALATE, not a defect.
  #
  # llama.cpp rather than vLLM: vLLM is built around GPU paged-attention and its CPU path is a large
  # install for a slow result. For a small always-on brain on cores you already have, llama.cpp is
  # the right tool. The dispatcher does not care either way - both are OpenAI-compatible URLs.
  brain:
    image: ghcr.io/ggml-org/llama.cpp:server
    container_name: agent-brain
    restart: unless-stopped

    # BACKPLANE ONLY. No exposed port. Nothing outside the stack talks to the brain, and the brain
    # cannot reach out - which matters more here than elsewhere, because inference is exactly the
    # kind of workload people accidentally point at the internet.
    networks:
      backplane:
        aliases:
          - brain.local

    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

    # The model comes off the STORAGE PLANE, not out of the image. Pull once, share between
    # instances, survive a rebuild. LANDMINE: an unknown HuggingFace repo returns 401 "Invalid
    # username or password", NOT 404 - so a typo in the model name reads as an auth failure and
    # sends you looking for a token you never needed.
    command: >
      -hf ${AGENT_BRAIN_MODEL:-Qwen/Qwen2.5-0.5B-Instruct-GGUF}
      --host 0.0.0.0 --port 8080
      -c ${AGENT_BRAIN_CTX:-4096}
      -t ${AGENT_BRAIN_THREADS:-4}
    volumes:
      - type: volume
        source: agent_storage
        target: /root/.cache/llama.cpp
        volume: { subpath: models }

  # ── DECIDED 2026-08-12: the keyed tier stays on the BRIDGE ────────────────────────────────────
  #
  # The alternative was `network_mode: "service:agent"` - mongo joining the agent's network
  # namespace, binding 127.0.0.1, on no network at all. It was measured rather than argued:
  #
  #     TCP over docker bridge     0.429 ms per find-by-key   <- what this file does
  #     loopback (shared netns)    0.368 ms
  #
  # 14%, not the 2-3x that would justify it. The real attraction was never speed: a shared namespace
  # makes mongo unreachable by every other container on the host, which is a genuine reduction in
  # attack surface.
  #
  # It loses on LIFECYCLE. A container joined to another's namespace dies with it - restart the
  # agent and mongo must restart too. That is the exact opposite of the property this whole design
  # is built around: the agent's life is not the connection's life, and its memory should outlive a
  # restart of its mind.
  #
  # So: bridge, and pay the 0.06 ms. If the reachability concern matters more in your deployment
  # than lifecycle independence, the swap is one line - but measure your own numbers first, because
  # these are from one host on one day.
  #
  # ── the KEYED tier ────────────────────────────────────────────────────────────────────────────
  # Deterministic recall: expand this crumb, what is X, what did I do in session S. A find-by-key,
  # ~1ms, no embedding and no model in the path.
  #
  # NOT A VECTOR STORE, and it must not become one. Community MongoDB has no ANN index. The
  # temptation is real because it is the database already sitting there; putting semantic search
  # here produces something that answers slowly and confidently with the wrong neighbour.
  mongo:
    image: mongo:7
    container_name: agent-mongo
    restart: unless-stopped

    # BACKPLANE ONLY. Not on the front panel, no published ports, and - because the backplane is
    # `internal: true` - no route off the host either. It cannot be reached from outside and it
    # cannot reach out. The only thing that can talk to it is the agent, which is the only other
    # thing on this plane.
    networks:
      backplane:
        aliases:
          - memory.local

    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

    volumes:
      # STATE IS THE BEING. The vessel rebuilds from a Dockerfile; this does not rebuild from
      # anything. Back it up OFF this box.
      - type: volume
        source: agent_storage
        target: /data/db
        volume: { subpath: mongo }
    healthcheck:
      # Speak the protocol. A port being open says nothing about whether the database answers.
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 20s

# ── TWO PLANES ───────────────────────────────────────────────────────────────────────────────────
# Modelled on a router chassis, where the backplane carrying RE-to-PFE traffic is never routed and
# never reachable from a revenue port. The control plane does not touch the data plane, by
# construction rather than by firewall rule.
#
#     backplane   internal: true. NO route in, NO route out. Memory lives here.
#     frontpanel  the only plane with exposure. The door lives here.
#     the agent   the ONLY thing on both - the routing engine between them.
#
# Why `internal: true` rather than "just do not publish a port": an unpublished container is still
# on a bridge with a default route. It can reach the internet, and anything else on that bridge can
# reach it. `internal` removes the route entirely, so a compromised dependency inside the memory
# tier has nowhere to call home TO. Segregation you can verify, not segregation you assert.
#
# VERIFY IT RATHER THAN TRUST THE FLAG:
#     docker compose exec mongo getent hosts github.com     -> must FAIL
#     docker compose exec agent  getent hosts github.com    -> resolves (it is on the front panel)
networks:
  backplane:
    driver: bridge
    internal: true          # <- the whole point. No gateway is attached to this network.
  frontpanel:
    driver: bridge

# ── THE STORAGE PLANE ────────────────────────────────────────────────────────────────────────────
# One place where persistent bytes live, backed by whatever the host actually has - an M.2, a USB
# enclosure, a PCI card, an array. Point AGENT_STORAGE at it once and every service in the stack
# extends onto it; move it to faster or bigger hardware by changing one path.
#
# THE RULE IT ENFORCES: the image carries CODE, the plane carries BYTES.
#
# This is a CORRECTION. The embedding model was baked into the image, taking it from 514MB to
# 1.31GB, on the argument that pre-connected beats network-fetched. That argument was right and the
# conclusion was wrong: a volume on local disk IS pre-connected. Baking bought nothing over mounting
# and cost image size on every pull, every instance, every rebuild - and made the model impossible
# to share between two agents on the same host.
#
# What belongs on the plane: models, keyed state, the vector store, the CA, the tattoo, the memory
# namespace. What belongs in the image: code, and the templates that scaffold a new store.
#
# What must NEVER be here: anything the agent cannot rebuild AND cannot afford to lose without a
# backup. This plane is the BEING. A vessel rebuilds from a Dockerfile; this does not rebuild from
# anything, so back it up off the box it runs on.
volumes:
  agent_storage:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${AGENT_STORAGE:-./storage}


    # The agent runs INSIDE a named multiplexer session, not as this container's foreground process.
    # That is what makes it detach-survivable: closing your terminal, dropping the VPN, or rebooting
    # YOUR machine are not events in its life. See agentboot/session.py.
    #
    # Order matters. Prove the hooks fire BEFORE holding the session open - a container that comes up
    # attachable but with no enforcement is a body with no mind, and it looks perfectly healthy.
    command:
      - bash
      - -lc
      - |
        set -e
        python3 -m agentboot install --prove
        exec zellij --layout /opt/agent-boot/examples/zellij/main.kdl --session "${AGENT_SESSION:-main}"

    healthcheck:
      # Two different claims, and both are required:
      #   verify  - the hooks still EXECUTE and emit their markers (configured is not working)
      #   attach  - the holding session is LIVE, not merely listed (an exited session still prints)
      test: ["CMD-SHELL",
             "python3 -m agentboot verify && python3 -m agentboot attach --session ${AGENT_SESSION:-main}"]
      interval: 60s
      timeout: 15s
      retries: 3
      start_period: 15s

# REACHING IT - one session, four addresses. The way in is interchangeable precisely because the
# agent does not depend on any of them:
#
#   local     docker exec -it agent-boot zellij attach main
#   remote    ssh -t <host> docker exec -it agent-boot zellij attach main
#   render    python3 -m agentboot attach --session main --host <host> --container agent-boot
#
# The -t / -it is NOT optional. Without a forced TTY the attach renders unusable garbage, which reads
# as "the agent is broken" when the agent is fine and you simply did not give it a terminal.
#
# The state directory is a bind mount by default so it is trivially backup-able and inspectable
# from the host. Point AGENT_STATE at a real backed-up path in production — the tattoo and the
# memory namespace live there, and they are the only things that cannot be rebuilt.
